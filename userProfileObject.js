// Create an object named userProfile to store user information
const userProfile = {
    // User's full name as a string
    fullName: "Princess Nicole Salazar", 
    
    // User's age as a number
    age: 22,              
    
    // User's favorite number as a number
    favoriteNumber: 11,    
    
    // User's favorite colors as an array of strings
    favoriteColors: ["blue", "green", "red"] 
};

// Log the entire userProfile object to the console
console.log(userProfile);